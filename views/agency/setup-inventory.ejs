<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Setup-Inventory</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container mt-5">
      <div class="row justify-content-center">
        <div class="col-md-8">
          <h2 class="text-center mb-4">Setup-Inventory</h2>

          <form
            action="/agency/<%= newAgency._id %>/setup-inventory"
            method="POST"
            class="needs-validation"
            novalidate
          >
            <div class="mb-3">
              <label for="currentCapacity" class="form-label"
                >Current Capacity (in kilos)</label
              >
              <input
                type="number"
                class="form-control"
                id="currentCapacity"
                name="inventory[currentCapacity]"
                required
              />
              <div class="invalid-feedback">
                Please provide Current Capacity
              </div>
            </div>

            <div class="mb-3">
              <label for="totalCapacity" class="form-label"
                >Total Capacity (in kilos)</label
              >
              <input
                type="number"
                class="form-control"
                id="totalCapacity"
                name="inventory[totalCapacity]"
                required
              />
              <div class="invalid-feedback">Please provide Total Capacity</div>
            </div>

            <div class="mb-3">
              <label for="address" class="form-label">Address</label>
              <input
                type="text"
                class="form-control"
                id="address"
                name="inventory[location][address]"
                required
              />
              <div class="invalid-feedback">Please provide Address</div>
            </div>

            <div class="mb-3">
              <label for="city" class="form-label">City</label>
              <input
                type="text"
                class="form-control"
                id="city"
                name="inventory[location][city]"
                required
              />
              <div class="invalid-feedback">Please provide city</div>
            </div>

            <div class="mb-3">
              <label for="state" class="form-label">State</label>
              <input
                type="text"
                class="form-control"
                id="state"
                name="inventory[location][state]"
                required
              />
              <div class="invalid-feedback">Please provide state</div>
            </div>

            <div class="mb-3">
              <label for="postalCode" class="form-label">Postal-Code</label>
              <input
                type="text"
                class="form-control"
                id="postalCode"
                name="inventory[location][postalCode]"
                required
              />
              <div class="invalid-feedback">Please provide Postal-Code</div>
            </div>

            <div class="col-12 mt-4">
              <button type="submit" class="btn btn-primary w-100">
                Setup-Inventory
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
